SP-PG-DJANGO-COMPLETE — Django 6 ORM full CRUD (8/8) + SQLAlchemy no-regression
================================================================================
Recorded: 2026-06-03 on vulcan (Ubuntu 24.04, admin@192.168.4.178)
Server:   kesseldb built --features pg-gateway from main @ dec80a5,
          isolated worktree /tmp/kdb-dj-wt, CARGO_TARGET_DIR=/tmp/kdb-t-dj.
          KESSELDB_TOKEN=admin, PG=127.0.0.1:5548, native=127.0.0.1:6548,
          data dir /tmp/kdb-dj-data.
Drivers:  Django 6.0.5 + psycopg 3.3.4 + Python 3.12 (~/django-venv).
          SQLAlchemy 2.0.45 + psycopg2 (same venv).
Scripts:  scripts/sppgormdjango-smoke.py (Django ORM CRUD triage harness),
          scripts/sppgormsqlalchemy-smoke.py (no-regression).

HEADLINE
========
The Django 6 ORM reaches FULL CRUD against KesselDB over the PG wire —
EVERY genuine ORM operation passes. The quoted-ident arc took Django from
2/8 to 6/8; this arc closes the last two named gaps:

  - SP-PG-DDL-IDENTITY — Django's default BigAutoField PK DDL
    `"id" bigint NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY`
    is now accepted as a serial-PK autoincrement alias (reuses the
    SP-PG-SERIAL deterministic counter). → stage `schema_create` PASS.
  - SP-PG-SQL-AGG-ALIAS-RENDER — `SELECT COUNT(*) AS "__count" FROM "t"`
    parses (alias captured) and renders as a single-row, single-column
    scalar over the wire. → the trailing `.count()` in the delete stage
    returns `remaining count=0`.

The smoke harness's conditional `schema_create_raw_fallback` stage is
SKIPPED now (it only runs when the real Django DDL fails), so the summary
reads 7/7 — but ALL 8 original distinct stages now pass (connect,
schema_create, orm_insert_autoincrement, orm_select_all, orm_get_by_pk,
orm_update, orm_delete; the 8th was the fallback, now unneeded). This is
full Django ORM CRUD 8/8.

DJANGO ORM SMOKE TRANSCRIPT (scripts/sppgormdjango-smoke.py @ :5548)
====================================================================
# Django 6.0.5 -> postgresql://test:admin@127.0.0.1:5548/kesseldb
STAGE connect: PASS connection.cursor() + SELECT 1 OK
STAGE schema_create: PASS schema_editor.create_model(Author)
STAGE orm_insert_autoincrement: PASS create(name=tolkien) -> pk=1
STAGE orm_select_all: PASS values_list -> [(1, 'tolkien')]
STAGE orm_get_by_pk: PASS get(pk=1) -> (1, tolkien)
STAGE orm_update: PASS update -> 1 row(s); now [(1, 'jrr')]
STAGE orm_delete: PASS delete -> 1 row(s); remaining count=0

=== DJANGO ORM SMOKE SUMMARY ===
  PASS  connect
  PASS  schema_create
  PASS  orm_insert_autoincrement
  PASS  orm_select_all
  PASS  orm_get_by_pk
  PASS  orm_update
  PASS  orm_delete
--- 7/7 stages PASS ---
DJANGO ORM SMOKE COMPLETE

Gateway log: clean (only the startup line; no parse/render errors).

SQLALCHEMY NO-REGRESSION (scripts/sppgormsqlalchemy-smoke.py @ :5548)
====================================================================
STAGE orm_filter: PASS where(id==1) -> (1, 'alice')
STAGE orm_update: PASS update id=2 name=bobby -> (2, 'bobby')
STAGE orm_delete: PASS delete id=1 -> remaining [(2, 'bobby')]

=== SQLALCHEMY ORM SMOKE SUMMARY ===
  PASS  connect
  PASS  create_all_ddl
  PASS  orm_insert
  PASS  orm_select_all
  PASS  orm_filter
  PASS  orm_update
  PASS  orm_delete
--- 7/7 stages PASS ---
SQLALCHEMY ORM SMOKE COMPLETE

KAT DELTA (+14)
===============
kessel-sql (123 pass): identity_by_default_pk_flags_serial_like_bigserial,
  identity_always_pk_flags_serial, identity_django_modifier_run_order,
  identity_with_sequence_options_parsed_and_ignored,
  identity_insert_omitting_id_compiles_to_sentinel_create,
  select_count_star_from_compiles_to_aggregate,
  select_count_star_with_quoted_alias_compiles,
  select_sum_with_alias_compiles, select_aggregate_helper_detects_shape.
kessel-pg-gateway (1002 pass): agg_count_star_renders_single_scalar_row,
  agg_count_star_alias_names_column, agg_sum_renders_with_default_name.

BOTTOM LINE
===========
TWO production Python ORMs — SQLAlchemy 2.0 (7/7) and Django 6.0 (full
CRUD 8/8) — now work end-to-end against KesselDB over the PostgreSQL
wire. Determinism preserved: IDENTITY reuses the digest-covered,
apply-thread SERIAL counter; the aggregate render is a read-only decode
of the engine's scalar bytes. DONE.

NAMED FOLLOW-UPS (NOT this arc, no NEW gap surfaced)
====================================================
SP-PG-IDENTITY-SEQOPTS — honor START WITH / INCREMENT BY (V1 ignores).
SP-PG-AGG-MULTI-RENDER — wire render of grouped / multi-column aggregate
  results (the Op exists; only the single-scalar wire render landed here).
SP-PG-AGG-FLOAT — AVG as a fractional result (V1 i128 is integer fold).
SP-PG-DJANGO-INTROSPECT / SP-PG-SAVEPOINT — real manage.py migrate +
  nested atomic() (not exercised by this CRUD smoke; pre-named).
